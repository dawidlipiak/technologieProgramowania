package org.example;

import java.util.List;

public class ElementDataBase {
    /**
     * Metoda dodajaca elementy faktury do bazy danych
     * @param elements lista elementow dodawanych do bazy danych
     */
    public static void addToElementDataBase(List<Element> elements) {
        /**
         * Dodajemy do bazy danych elementow element.get(i).getArticleName(),
         * element.get(i).getArticlePrize() oraz element.get(i).amount oraz element.get(i).vat
         */
    }
}
